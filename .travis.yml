language: node_js
node_js:
  - 16
jobs:
  include:
    - stage: Build
      name: Webpack compile
      env:
        - NODE_ENV=production
      script: yarn run build
    - stage: Lint
      name: ESLint run
      script: yarn lint
    - stage: Tests
      name: Unit tests
      script: yarn test
